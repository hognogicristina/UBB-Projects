adaugaE(L=[l1,l2,...,ln], e) = {
	[e] if len(L)==0
	l1 U adaugaE(l2,...,ln, e), otherwise
}

concatR(L1=[l1,l2,...,ln], L2=[e1,e2,...,en]) = {
	L2, if len(L1)==0
	L1, if len(L2)==0
	l1 U concatR(L1=[l2,...,ln], L2=[e1,...,en]), otherwise
}